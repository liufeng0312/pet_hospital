server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pet_hospital?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: feng0312
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  
  security:
    user:
      name: admin
      password: admin123

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.pethospital.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.pethospital: debug

# 微信小程序配置
wechat:
  appid: test_appid  # 替换为真实的微信小程序AppID
  secret: test_secret  # 替换为真实的微信小程序Secret

# 文件上传配置
file:
  upload-path: uploads  # 文件上传路径（相对路径，会在项目根目录创建）


# JWT配置
jwt:
  secret: pet-hospital-jwt-secret-key-change-this-in-production-256-bits
  expiration: 604800000  # 7天（毫秒）
  header: Authorization
  prefix: "Bearer "
